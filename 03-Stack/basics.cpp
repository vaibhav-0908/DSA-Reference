//declaration (LIFO)
stack<int> st;
st.push(1);
st.emplace(5);
cout<< st.top(); //prints 5; index accessing not allowed in stack
st.pop();//pops 5

//swapping s1 with s2
stack<int> st1,st2;
st1.swap(s2);
